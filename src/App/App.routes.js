class Routes {
    /** @returns {string} `/` */
    root = () => '/';

    /** @returns {string} `/login` */
    login = () => '/login';

    /** @returns {string} `/index` */
    index = () => '/index';
}

export const routes = new Routes();
